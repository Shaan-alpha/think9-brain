import type { Metadata } from "next";
import type { ReactNode } from "react";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Think9 Brain",
  description:
    "Answers operational questions from the documents a company already has, cites the section it used, and declines when the corpus cannot support an answer. Synthetic corpus.",
};

/* Props typed explicitly rather than with Next's generated `LayoutProps` global, which
   lives in gitignored build output — it typechecks locally and fails on a clean clone. */
export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
