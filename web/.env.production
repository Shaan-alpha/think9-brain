# Read at build time by Next. NEXT_PUBLIC_* values are inlined into the bundle, so this
# has to be present when the production build runs — it is not a runtime setting.
# Not a secret: it is the public address of the API the browser calls.
NEXT_PUBLIC_BACKEND_URL=https://think9-brain-api.onrender.com
